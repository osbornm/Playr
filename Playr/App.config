<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <startup>
    <supportedRuntime version="v4.0" sku=".NETFramework,Version=v4.5" />
  </startup>
  <appSettings>
    <!-- Override this setting to control the URL for Playr. The default is port 5555. -->
    <add key="Playr:Url" value="http://localhost:5555/" />
    <!-- Override this setting to control the number of the songs in the playr queue. -->
    <add key="Playr:QueueLength" value="10"/>
    <!-- Fanart API Key -->
    <add key="Playr:FanartApiKey" value=""/>
    <!--
    Use this key to turn on the embedded RavenDB server. Can be useful for data spelunking during debug.
    Note that the embedded server is always on with Debug builds of the application; this flag only
    affects Release builds. Embedded RavenDB always runs on port 8080.
    -->
    <add key="RavenDB:Embedded" value="false" />
  </appSettings>
</configuration>